# Prisma - Local dev (SQLite file)
# For D1: use `npm run db:migrate:remote` to apply migrations
DATABASE_URL="file:./dev.db"

# SecretVault 加解密主金鑰 (AES-256 需 32 bytes)
# 產生方式：openssl rand -base64 32
# Workers 部署時使用：wrangler secret put MASTER_KEY
# MASTER_KEY=""
